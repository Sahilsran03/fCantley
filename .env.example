VITE_API_BASE_URL=http://localhost:8080/api
# Public Google OAuth Web client ID; authorize the frontend origin in Google Cloud.
# Restart Vite (or rebuild for production) after setting this value. Never add a client secret.
VITE_GOOGLE_CLIENT_ID=